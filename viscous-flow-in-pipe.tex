\documentclass[12pt,tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{scopes}
\usepackage{verbatim}
\usetikzlibrary{calc,angles,patterns,quotes,positioning,shapes}
\usetikzlibrary{patterns.meta}
\usepackage{pgfplots}
\usepackage{pgf}
\usepackage[dvipsnames]{xcolor}
\usetikzlibrary{math}
\usetikzlibrary{backgrounds}
\pgfplotsset{compat=newest}
\pgfplotsset{plot coordinates/math parser=false}



\begin{document}

\def\down{-90}
\def\ang{30}
\def\hgt{10em}
\def\lwdth{0.2em}
\def\R0{10.7em}
\def\r0{2.7em}
\def\dotr{0.1em}
\def\parcelangle{30}

\def\totalWaterLengthX{45em}
\def\totalWaterHeightZ{21em}
\def\wallWidth{1em}
\def\parcelHalfHeight{0.2*\totalWaterHeightZ}


\begin{tikzpicture}[
    force/.style={>=latex,draw=blue,fill=blue,line width=\lwdth},
    axis/.style={>=latex,dashed,gray, line width=0.1em, 
                 dash pattern=on 8pt off 3pt},
    M/.style={rectangle,draw,fill=lightgray,minimum size=0.5cm,thin},
    m/.style={rectangle,draw=black,fill=lightgray,minimum size=0.3cm,thin},
    plane/.style={draw=black,fill=blue!10},
    string/.style={draw=red, thick},
    pulley/.style={thick},
    point/.style={inner sep=0, minimum width=0.4em, fill=black, text=black},
    timber-fill/.style={pattern={
        Lines[distance=8.5pt, angle=45]},
        pattern tile/.style={clip=false},
        pattern color=black
    },
    timber-fill-top/.style={
        pattern={Lines[distance=8.5pt, angle=135]}, 
        pattern tile/.style={clip=false},
        pattern color=black
    }, scale=3
]


    \tikzstyle{every node}=[font=\Large, text=black];

    
     \node[cylinder, draw, axis, violet, line width=0.1em, shape aspect=4, 
          cylinder uses custom fill, cylinder end fill=blue!50, 
          minimum height=11em, minimum width=3.5em,
          cylinder body fill=blue!25, opacity=0.5, 
          scale=3, rotate=0] (interior) {};
 
    
    \node[cylinder, draw, shape aspect=6, 
          cylinder uses custom fill, cylinder end fill=blue!50, 
          minimum height=12em, minimum width=8em,
          cylinder body fill=blue!25, opacity=0.4, 
          scale=3, rotate=0, line width=0.15em] (exterior) at ([xshift=-0.5em]interior.center) {};



    \path (interior.before top) -- (interior.after top) node (centerRight) [midway] {}; 
    \path (interior.before bottom) -- (interior.after bottom) node (centerLeft) [midway] {}; 
    \draw[axis] (centerLeft.center) -- (centerRight.center);
    \draw[->, force] (centerRight.center) -- ++(3em, 0) node[above right=0ex, inner sep=0]{$v(r=0)$};
    

    \draw[axis, opacity=0.5, line width=0.1em] ([yshift=-0.08em]interior.after bottom) to[out=-5, in=5] ([yshift=0.08em]interior.before bottom);
    \draw[axis, opacity=0.5, line width=0.1em] ([yshift=-0.08em]exterior.after bottom) to[out=-5, in=5] ([yshift=0.08em]exterior.before bottom);


    \draw[axis, ->] (centerRight.center) -- ([yshift=-0.08em]interior.before top) node[above right=1ex, anchor=south, inner sep=0]{$r$};
    \draw[axis, ->] (centerLeft.center) -- ([yshift=-0.08em]exterior.after bottom) node[above=1ex, anchor=south, inner sep=0]{$R$};
    
    \draw[force, ->] (interior.shape center) -- ++(2em, 0) node[midway, anchor=south west]{$f_p$};
    \draw[force, ->] (interior.shape center) -- ++(-2em, 0) node[midway, anchor=north east]{$f_{\tau}$};
    \node[circle,point,draw=black] at (interior.shape center) {};


    \draw (exterior.before bottom)  -- ++(0, -0.5em) node [inner sep=0, midway] (leftEdgeLengthLabel){};
    \draw (exterior.after top) -- ++(0, -0.5em)  node [inner sep=0, midway] (rightEdgeLengthLabel){};
    \draw[axis, <->] (leftEdgeLengthLabel.center) -- (rightEdgeLengthLabel.center) node[midway, anchor=north]{$l$};


    \node[circle,point,draw=black] at (centerRight.center) {};
    \node[circle,point,draw=black] at (centerLeft.center) {};

\end{tikzpicture}

\end{document} 